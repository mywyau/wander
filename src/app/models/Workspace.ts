// types/Workspace.ts

export interface Workspace {
    name: string;
    street: string;
    city: string;
    state: string;
    postcode: string;
    priceModel:string
    hourPrice: number;
    dayPrice: number;
    weekPrice: number;
    monthPrice: number;
    annualPrice: number;
  }
  